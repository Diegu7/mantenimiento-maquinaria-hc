<tr>
	<td><%= image_tag(user.image_url.to_s, style: 'max-width:10%;') %></td>
    <td><%= user.full_name %></td>
	<td><%= user.username %></td>
	<td><%= user.email %></td>
	<td>
		<div class="input-group-btn">
			<%= link_to edit_user_path(user), class: "btn bg-blue btn-flat" do %>
				<span class="fa fa-pencil"></span>
				<font>Editar</font>
			<% end %>

			<%= link_to user, class: "btn bg-red btn-flat", method: :delete, data: { confirm: "¿Seguro que deseas borrar #{user.username}?" } do %>
				<span class="fa fa-trash"></span>
				<font>Borrar</font>
			<% end %>
		</div>
	</td>
</tr>
