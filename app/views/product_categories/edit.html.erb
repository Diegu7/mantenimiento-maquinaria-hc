<section class="content-header">
  <h1>
    Categorías de Productos
    <small>Editar</small>
  </h1>

  <ol class="breadcrumb">
		<li><i class="fa fa-cog"></i> <span>Categorías</span></li>
		<li><%= link_to "Categorías", product_categories_path %></li>
    <li><%= @product_category.name %></li>
		<li class="active">Editar</li>
	</ol>
  <%= render partial: "shared/errors", locals: { obj: @product_category } %>
</section>

<section class="content">
  <div class="row">
    <div class="col-md-6">
      <div class="box box-success">
        <div class="box-body">
          <%= form_for(@product_category) do |f| %>
            <%= render partial: "shared/name_form", locals: {f: f} %>
            <div class="input-group-btn">
              <%= link_to "Cancelar", product_categories_path, class: "btn bg-red btn-flat" %>
              <%= f.submit "Actualizar", class: "btn bg-green btn-flat" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>