<section class ="content-header" >
  <h1>
    Areas de Mantenimiento
    <small>Nueva</small>
  </h1>

  <ol class="breadcrumb">
		<li><i class="fa fa-cog"></i> <span>Areas</span></li>
		<li><%= link_to "Areas de Mantenimiento", machine_areas_path %></li>
		<li class="active">Nueva</li>
	</ol>
  <%= render partial: "shared/errors", locals: { obj: @machine_area } %>
</section>

<section class="content">
  <div class="row">
    <div class="col-md-6">
      <div class="box box-success">
        <div class="box-body">
          <%= form_for(@machine_area) do |f| %>
            <%= render partial: "shared/name_form", locals: {f: f} %>
            <div class="input-group-btn">
              <%= link_to "Cancelar", machine_areas_path, class: "btn bg-red btn-flat" %>
              <%= f.submit "Crear", class: "btn bg-green btn-flat" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>