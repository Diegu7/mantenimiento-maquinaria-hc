<tr>
	<td><%= machine_area.name %></td>
	<td>
		<div class="input-group-btn">
			<%= link_to edit_machine_area_path(machine_area), class: "btn bg-blue btn-flat" do %>
				<span class="fa fa-pencil"></span>
				<font>Editar</font>
			<% end %>

			<%= link_to machine_area, class: "btn bg-red btn-flat", method: :delete, data: { confirm: "¿Seguro que deseas borrar #{machine_area.name}?" } do %>
				<span class="fa fa-trash"></span>
				<font>Borrar</font>
			<% end %>
		</div>
	</td>
</tr>
